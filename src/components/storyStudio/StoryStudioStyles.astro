---
/** 小说创作共用样式 */
---
<style is:global>
  /* 主栏全宽铺满；禁止整页 max-width 留死区（正文可读宽度在内部列约束） */
  .story-studio-panel {
    width: 100%;
    max-width: none;
    display: flex;
    flex-direction: column;
    min-width: 0;
    min-height: 0;
    height: 100%;
    max-height: none !important;
    overflow: hidden;
  }
  .story-studio-panel > h2,
  .story-studio-panel > .ss-tip,
  .story-studio-panel > .ss-manage-header,
  .story-studio-panel > .ss-toolbar,
  .story-studio-panel > .ss-manage-status,
  .story-studio-panel > .form-group,
  .story-studio-panel > .ss-status {
    flex-shrink: 0;
  }
  .story-studio-panel h2 {
    margin: 0 0 6px;
  }
  .ss-tip {
    font-size: 0.78rem;
    color: var(--color-text-muted);
    margin: 0 0 12px;
    line-height: 1.45;
  }
  .ss-manage-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 12px;
    flex-shrink: 0;
  }
  .ss-manage-heading {
    min-width: 0;
    flex: 1;
  }
  .ss-manage-heading h2 {
    margin: 0 0 6px;
  }
  .ss-manage-heading .ss-tip {
    margin: 0;
  }
  .ss-manage-toolbar {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    align-items: center;
    flex-shrink: 0;
  }
  .ss-manage-toolbar .btn {
    width: auto;
    min-width: 0;
    margin: 0;
  }
  .ss-toolbar {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    align-items: center;
    margin-bottom: 12px;
    flex-shrink: 0;
  }
  .ss-toolbar .btn {
    width: auto !important;
    margin: 0 !important;
  }
  .ss-status {
    font-size: 0.75rem;
    color: var(--color-text-muted);
    min-height: 1.2em;
    margin-top: 8px;
    flex-shrink: 0;
  }
  .ss-manage-status {
    display: none;
    padding: 8px 12px;
    font-size: 0.78rem;
    border-radius: 8px;
    margin-bottom: 12px;
    border: 1px solid var(--color-border-subtle);
    background: var(--color-surface-elevated);
    color: var(--color-text-secondary);
    flex-shrink: 0;
  }
  .ss-manage-status:not([hidden]) {
    display: block;
  }
  .ss-novel-list {
    flex: 1 1 auto;
    min-height: 0;
    overflow-x: hidden;
    overflow-y: auto;
    scrollbar-gutter: stable;
  }
  .ss-empty, .ss-muted {
    color: var(--color-text-muted);
    font-size: 0.78rem;
  }
  .ss-empty,
  .ss-empty--panel {
    margin: auto;
    padding: 28px 18px;
    max-width: 28rem;
    width: calc(100% - 24px);
    text-align: center;
    font-size: 0.78rem;
    font-weight: 500;
    line-height: 1.55;
    border: 1px dashed var(--color-accent-border);
    border-radius: 8px;
    background: var(--color-accent-soft);
    box-sizing: border-box;
  }
  .ss-card {
    margin-bottom: 0;
    border-radius: var(--radius-md, 10px);
    border: 1px solid var(--color-focus-ring);
    background: var(--color-surface-inset);
    display: flex;
    flex-direction: column;
    min-height: 0;
    flex: 1 1 0;
    overflow: hidden;
  }
  .ss-card-head {
    display: flex;
    justify-content: space-between;
    gap: 12px;
    align-items: center;
    padding: 10px 14px;
    border-bottom: 1px solid var(--color-accent-soft);
    flex-shrink: 0;
  }
  .ss-card-head strong { font-size: 0.88rem; color: var(--color-text); }
  .ss-card-head span { font-size: 0.72rem; color: var(--color-text-muted); }
  .ss-card-body {
    padding: 12px 14px;
    flex: 1 1 auto;
    min-height: 0;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
  }
  .ss-graph-stack {
    flex: 1 1 auto;
    min-height: 0;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    overflow: hidden;
  }
  @media (max-width: 900px) {
    .ss-graph-stack {
      grid-template-columns: 1fr;
      overflow-y: auto;
    }
  }

  .ss-novel-item {
    display: flex;
    flex-direction: column;
    gap: 0;
    padding: 0;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm, 8px);
    background: var(--color-surface);
    margin-bottom: 8px;
    overflow: hidden;
  }
  .ss-novel-item.is-active {
    border-color: var(--color-accent-border);
    background: var(--color-accent-soft);
  }
  .ss-novel-item__main {
    display: block;
    padding: 12px 14px;
    min-width: 0;
  }
  .ss-novel-title {
    display: block;
    width: 100%;
    margin: 0;
    padding: 0;
    border: none;
    background: transparent;
    text-align: left;
    font: inherit;
    font-size: 0.92rem;
    font-weight: 650;
    color: var(--color-text);
    cursor: pointer;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .ss-novel-title:hover {
    color: var(--color-accent-hover);
  }
  .ss-novel-meta {
    display: block;
    width: 100%;
    margin: 2px 0 0;
    padding: 0;
    border: none;
    background: transparent;
    text-align: left;
    font: inherit;
    font-size: 0.72rem;
    color: var(--color-text-muted);
    cursor: pointer;
    line-height: 1.4;
  }
  .ss-novel-meta:hover {
    color: var(--color-text-secondary);
  }
  .ss-novel-item__actions {
    display: flex;
    flex-wrap: nowrap;
    align-items: center;
    gap: 4px;
    padding: var(--space-1, 4px) var(--space-2, 8px) var(--space-2, 8px);
    border-top: 1px solid var(--color-border-subtle);
    background: var(--color-surface-inset);
    overflow-x: auto;
    scrollbar-width: thin;
  }
  .ss-novel-item__actions-group {
    display: flex;
    align-items: center;
    gap: 4px;
    flex-shrink: 0;
  }
  .ss-novel-item__share {
    display: flex;
    flex-wrap: nowrap;
    align-items: center;
    gap: 4px;
    margin-left: auto;
    flex-shrink: 0;
  }
  .ss-novel-icon.ss-novel-icon--export:hover {
    color: var(--color-accent-hover);
  }
  .ss-novel-icon.ss-novel-icon--danger:hover,
  .ss-novel-icon.btn-icon--danger:hover {
    color: var(--color-danger);
  }

  .ss-graph-row {
    display: grid;
    grid-template-columns: 96px 1fr 1.2fr auto;
    gap: 6px;
    margin-bottom: 6px;
    align-items: center;
  }
  #ssGraphEdges .ss-graph-row {
    grid-template-columns: 1fr 100px 1fr auto;
  }
  .ss-graph-row input,
  .ss-graph-row select {
    width: 100%;
    min-width: 0;
  }

  .ss-outline-dir {
    margin-bottom: 10px;
  }
  .ss-outline-list {
    flex: 1 1 auto;
    min-height: 120px;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .ss-outline-item {
    border: 1px solid var(--color-border-subtle);
    border-radius: 8px;
    padding: 10px;
    background: var(--color-surface);
    flex-shrink: 0;
  }
  .ss-outline-item__head {
    display: flex;
    gap: 8px;
    align-items: center;
    margin-bottom: 6px;
    flex-wrap: wrap;
  }
  .ss-ol-idx {
    font-size: 0.72rem;
    color: var(--color-text-muted);
    min-width: 2rem;
  }
  .ss-ol-title { flex: 1; min-width: 0; }
  .ss-ol-summary { width: 100%; resize: vertical; }

  .ss-write-grid {
    display: grid;
    gap: 10px;
    flex: 1 1 auto;
    min-height: 0;
    overflow-y: auto;
    grid-template-columns: 1fr 1fr;
    align-content: start;
  }
  .ss-write-grid .form-group:nth-child(3),
  .ss-write-grid .form-group:nth-child(4),
  .ss-write-grid .form-group:nth-child(5),
  .ss-write-grid > .novel-switch,
  .ss-write-grid > .ss-warn {
    grid-column: 1 / -1;
  }
  .ss-write-grid #ssWriteChapterContent {
    min-height: 220px;
    max-width: min(52rem, 100%);
  }
  @media (max-width: 720px) {
    .ss-write-grid { grid-template-columns: 1fr; }
  }
  .ss-warn {
    margin-top: 0;
    padding: 8px 10px;
    border-radius: 8px;
    border: 1px solid var(--color-warning-border);
    background: var(--color-warning-soft);
    color: var(--color-text-secondary);
    font-size: 0.78rem;
  }

  .ss-read-layout {
    display: grid;
    grid-template-columns: 200px minmax(0, 1fr);
    gap: 12px;
    flex: 1 1 auto;
    min-height: 0;
    overflow: hidden;
  }
  .ss-read-aside {
    display: flex;
    flex-direction: column;
    min-height: 0;
    overflow: hidden;
  }
  @media (max-width: 720px) {
    .ss-read-layout {
      grid-template-columns: 1fr;
      overflow-y: auto;
    }
    .ss-graph-row,
    #ssGraphEdges .ss-graph-row {
      grid-template-columns: 1fr;
    }
  }
  .ss-toc {
    display: flex;
    flex-direction: column;
    gap: 4px;
    max-height: none;
    flex: 1;
    min-height: 0;
    overflow-y: auto;
  }
  .ss-toc-item {
    text-align: left;
    padding: 6px 8px;
    border: 1px solid transparent;
    border-radius: 6px;
    background: transparent;
    color: var(--color-text-secondary);
    font-size: 0.78rem;
    cursor: pointer;
    font-family: inherit;
  }
  .ss-toc-item:hover { background: var(--color-accent-soft); }
  .ss-toc-item.is-active {
    border-color: var(--color-accent-border);
    background: var(--color-accent-soft);
    color: var(--color-text);
  }
  .ss-read-stage {
    border: 1px solid var(--color-border);
    border-radius: 10px;
    background: var(--color-surface);
    min-height: 0;
    height: 100%;
    display: flex;
    flex-direction: column;
    overflow: hidden;
  }
  .ss-read-stage:fullscreen {
    background: var(--color-paper);
    padding: 24px;
  }
  .ss-read-body {
    flex: 1;
    overflow-y: auto;
    padding: 16px 18px;
    touch-action: pan-y;
  }
  .ss-read-page {
    max-width: min(42rem, 100%);
    margin: 0 auto;
  }
  .ss-read-page h3 {
    margin: 0 0 10px;
    font-size: 1.05rem;
  }
  .ss-read-summary-tag {
    font-size: 0.72rem;
    color: var(--color-warning);
    margin: 0 0 8px;
  }
  .ss-read-text {
    line-height: 1.75;
    font-size: 0.95rem;
    color: var(--color-text);
    white-space: pre-wrap;
  }
  .ss-read-nav {
    display: flex;
    gap: 8px;
    align-items: center;
    justify-content: space-between;
    padding: 10px 12px;
    border-top: 1px solid var(--color-border-subtle);
    flex-shrink: 0;
  }
  .ss-bm-item {
    display: inline-block;
    margin: 2px 4px 2px 0;
    padding: 4px 8px;
    font-size: 0.72rem;
    border-radius: 6px;
    border: 1px solid var(--color-border);
    background: var(--color-surface-inset);
    color: var(--color-text-secondary);
    cursor: pointer;
    font-family: inherit;
  }
</style>
